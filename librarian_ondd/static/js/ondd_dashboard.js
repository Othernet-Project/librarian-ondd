// Generated by CoffeeScript 1.10.0
(function(window, $, templates) {
  var errorMessage, onddForm, removeMessage, submitData, url;
  onddForm = $('#ondd-form');
  url = onddForm.attr('action');
  errorMessage = templates.onddSettingsError;
  removeMessage = function() {
    return (onddForm.find('.o-form-message')).slideUp(function() {
      return ($(this)).remove();
    });
  };
  submitData = function(data) {
    var res;
    res = $.post(url, data);
    res.done(function(data) {
      onddForm.html(data);
      ($(window)).trigger('transponder-updated');
      return setTimeout(removeMessage, 5000);
    });
    return res.fail(function() {
      return onddForm.prepend(errorMessage);
    });
  };
  return onddForm.on('submit', function(e) {
    var data;
    e.preventDefault();
    data = onddForm.serialize();
    return submitData(data);
  });
})(this, this.jQuery, this.templates);
