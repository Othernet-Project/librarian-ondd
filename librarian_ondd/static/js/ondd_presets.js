// Generated by CoffeeScript 1.10.0
(function(window, $, templates) {
  var FIELDS, customSettingsFields, f, fields, help, i, len, onTransponderSwitch, onddForm, options, transponders;
  FIELDS = ['frequency', 'symbolrate', 'polarization', 'delivery', 'modulation'];
  onddForm = $('#ondd-form');
  if (!onddForm.length) {
    return;
  }
  fields = {};
  for (i = 0, len = FIELDS.length; i < len; i++) {
    f = FIELDS[i];
    fields[f] = $("#" + f);
  }
  customSettingsFields = onddForm.find('.settings-fields');
  transponders = onddForm.find('#transponders');
  help = transponders.next('.o-field-help-message');
  customSettingsFields.hide();
  options = {};
  (transponders.find('option')).each(function() {
    var opt;
    opt = $(this);
    options[opt.val()] = opt;
  });
  onTransponderSwitch = function() {
    var coverage, data, j, len1, opt, val;
    val = transponders.val();
    opt = options[val];
    help.text('');
    if (!val) {
      customSettingsFields.hide();
    } else if (val === '-1') {
      customSettingsFields.show();
    } else {
      customSettingsFields.hide();
      data = opt.data();
      coverage = data.coverage;
      help.text(coverage);
      for (j = 0, len1 = FIELDS.length; j < len1; j++) {
        f = FIELDS[j];
        fields[f].val(data[f]);
      }
    }
  };
  transponders.on('change', onTransponderSwitch);
  onTransponderSwitch();
})(this, this.jQuery, this.templates);
