// Generated by CoffeeScript 1.10.0
(function(window, $, templates) {
  var FIELDS, f, fields, formFields, i, len, lnbSettings, options, presets, presetsContainer, transponders;
  FIELDS = ['frequency', 'symbolrate', 'polarization', 'delivery', 'modulation'];
  lnbSettings = $('.lnb-settings');
  formFields = $('.settings-fields');
  presets = templates.satPresets;
  if (!formFields.length) {
    return;
  }
  presetsContainer = $('<div>');
  lnbSettings.after(presetsContainer);
  fields = {};
  for (i = 0, len = FIELDS.length; i < len; i++) {
    f = FIELDS[i];
    fields[f] = $("#" + f);
  }
  presetsContainer.html(presets);
  formFields.hide();
  transponders = presetsContainer.find('#transponders');
  options = {};
  transponders.find('option').each(function() {
    var opt;
    opt = $(this);
    return options[opt.val()] = opt;
  });
  return transponders.on('change', function() {
    var data, j, len1, opt, results, val;
    val = transponders.val();
    opt = options[val];
    if (val === '0') {

    } else if (val === '-1') {
      return formFields.show();
    } else {
      formFields.hide();
      data = opt.data();
      results = [];
      for (j = 0, len1 = FIELDS.length; j < len1; j++) {
        f = FIELDS[j];
        results.push(fields[f].val(data[f]));
      }
      return results;
    }
  });
})(this, this.jQuery, this.templates);
